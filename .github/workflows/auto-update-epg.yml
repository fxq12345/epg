name: EPG自动更新并同步到Gitee（酷9适配版）

on:
  schedule:
    - cron: '0 17 * * *'  # 每天UTC 17:00（北京时间次日01:01）自动运行
  workflow_dispatch:     # 允许手动触发

jobs:
  build-and-sync:
    runs-on: ubuntu-latest
    steps:
      - name: 拉取GitHub代码仓库
        uses: actions/checkout@v4
        with:
          ref: 'master'
          fetch-depth: 0
          clean: true
          token: ${{ secrets.GITHUB_TOKEN }}

      - name: 配置Python环境
        uses: actions/setup-python@v5
        with:
          python-version: '3.10'
          cache: 'pip'

      - name: 安装依赖包
        run: |
          python -m pip install --upgrade pip
          # 注意：爬虫需要 beautifulsoup4，合并脚本可能需要其他库，这里一并安装
          pip install requests lxml beautifulsoup4

      # =============== ⭐ 新增步骤：运行潍坊EPG爬虫 (带容错) ⭐ ===============
      - name: 🕷️ 运行潍坊EPG爬虫 (失败仅提示)
        run: |
          mkdir -p output
          python weifang_epg_spider.py || echo "⚠️ 潍坊EPG抓取失败，将使用旧数据或空数据继续流程"
        env:
          PYTHONIOENCODING: utf-8
        # 关键配置：即使这一步报错，也继续执行后续步骤
        continue-on-error: true
      # ===================================================

      - name: 运行EPG合并脚本
        run: python merge.py
        env:
          PYTHONIOENCODING: utf-8

      - name: 检查输出文件
        run: |
          mkdir -p output
          if [ ! -f "output/epg.xml" ] || [ ! -f "output/epg.gz" ]; then
            echo "❌ 目标文件生成失败"
            exit 1
          else
            echo "✅ 目标文件生成成功"
          fi

      - name: 提交并推送更新到GitHub
        run: |
          set -x
          git config --global user.name "GitHub Actions"
          git config --global user.email "actions@github.com"
          # 先强制拉取远程最新代码，解决分支不一致问题
          git pull origin master --force
          git rm -r --cached output/epg.xml output/epg.gz || true
          git add -f output/epg.xml output/epg.gz epg_merge.log
          if git diff --cached --quiet; then
            echo "ℹ️ 无EPG文件变更，跳过GitHub提交"
          else
            git commit -m "自动更新EPG数据（$(date +'%Y-%m-%d %H:%M:%S')）"
            # 推送失败仅提示，不中断流程
            if git push origin master --force; then
              echo "✅ GitHub推送成功"
            else
              echo "⚠️ GitHub推送失败"
            fi
          fi
          set +x
        continue-on-error: true

      - name: 同步更新到Gitee仓库（SSH方式）
        env:
          GITEE_SSH_PRIVATE_KEY: ${{ secrets.GITEE_SSH_PRIVATE_KEY }}
          GITEE_REPO: "git@gitee.com:room-efficiency/epg.git"
        run: |
          set -x
          # 配置SSH
          mkdir -p ~/.ssh
          echo "$GITEE_SSH_PRIVATE_KEY" > ~/.ssh/id_ed25519
          chmod 600 ~/.ssh/id_ed25519
          ssh-keyscan gitee.com >> ~/.ssh/known_hosts
          # 添加Gitee远程仓库
          git remote add gitee $GITEE_REPO || true
          # 强制推送
          if git push gitee master --force; then
            echo "✅ 成功同步到Gitee仓库"
          else
            echo "⚠️ Gitee同步失败"
          fi
          set +x
        continue-on-error: true

      - name: 清理工作目录
        if: always()
        run: |
          rm -rf __pycache__
          rm -f epg_merge.log
          rm -rf output/__pycache__
