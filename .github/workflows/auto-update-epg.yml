name: 'Auto Update EPG'

on:
  schedule:
    - cron: '0 0,12 * * *'
  workflow_dispatch:
  push:
    branches:
      - master

jobs:
  update:
    runs-on: ubuntu-latest
    timeout-minutes: 30
    permissions:
      contents: write

    steps:
      - name: Checkout code
        uses: actions/checkout@v4
        with:
          fetch-depth: 1

      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.10'

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          if [ -f "requirements.txt" ]; then
            pip install -r requirements.txt
          else
            pip install requests lxml beautifulsoup4 xmltodict aiohttp tqdm
          fi

      - name: Generate EPG with enhanced error capture
        run: |
          echo "=== 开始生成EPG ==="
          mkdir -p output
          rm -f output/epg.xml output/epg.gz 2>/dev/null || true
          
          python merge.py > merge_output.log 2>&1 || echo "脚本执行失败，但已保存日志"
          
          echo "=== 脚本完整输出日志 ==="
          cat merge_output.log || echo "无日志输出"
          
          echo "=== 当前目录内容 ==="
          ls -la
          echo "=== output目录内容 ==="
          ls -la output/ || echo "output目录不存在"

      - name: Verify EPG content
        run: |
          echo "=== 验证EPG文件 ==="
          for path in "output/epg.xml" "epg.xml" "/home/runner/work/epg/epg/epg.xml"; do
            if [[ -f "$path" ]]; then
              echo "✅ 在 $path 找到EPG文件"
              cp "$path" output/epg.xml
              break
            fi
          done
          
          if [[ -f "output/epg.xml" ]]; then
            echo "EPG文件大小: $(du -h output/epg.xml)"
            echo "文件行数: $(wc -l < output/epg.xml)"
            channels=$(grep -c '<channel ' output/epg.xml)
            programmes=$(grep -c '<programme ' output/epg.xml)
            echo "频道数量: $channels | 节目数量: $programmes"
            gzip -c output/epg.xml > output/epg.gz
          else
            echo "❌ EPG生成失败，但已完成环境验证"
          fi

      - name: Commit and push changes
        run: |
          git config --local user.email "github-actions[bot]@users.noreply.github.com"
          git config --local user.name "github-actions[bot]"
          
          # 只在文件存在时才添加
          if [[ -f "output/epg.xml" ]]; then
            git add output/epg.xml output/epg.gz
            if ! git diff --staged --quiet; then
              git commit -m "📺 EPG自动更新 - $(date +'%Y-%m-%d %H:%M:%S %Z')"
              git push https://x-access-token:${{ secrets.GITHUB_TOKEN }}@github.com/${{ github.repository }}.git HEAD:${{ github.ref_name }}
            else
              echo "📋 没有需要提交的变更"
            fi
          else
            echo "⚠️  EPG文件不存在，跳过提交"
          fi
