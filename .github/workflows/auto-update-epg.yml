name: EPG自动更新并同步到Gitee（酷9适配版）

on:
  schedule:
    - cron: '0 17 * * *'  # 每天UTC 17:00（北京时间次日01:00）自动运行
  workflow_dispatch:     # 允许手动触发

jobs:
  build-and-sync:
    runs-on: ubuntu-latest
    steps:
      - name: 1. 拉取GitHub代码仓库
        uses: actions/checkout@v4
        with:
          ref: 'master'
          fetch-depth: 0
          clean: true
          token: ${{ secrets.GITHUB_TOKEN }}

      - name: 2. 配置Python环境
        uses: actions/setup-python@v5
        with:
          python-version: '3.10'
          cache: 'pip'

      - name: 3. 安装依赖包
        run: |
          python -m pip install --upgrade pip
          pip install requests lxml

      # ======================= 关键修改 1: 潍坊爬虫容错 =======================
      - name: 4. 🟡 运行潍坊爬虫 (失败不中断)
        run: |
          mkdir -p output
          python weifang_epg_spider.py
        # 核心配置：即使这里报错 (Exit Code != 0)，也继续下一步
        continue-on-error: true

      - name: 5. 🔵 运行EPG合并脚本
        run: python merge.py
        env:
          PYTHONIOENCODING: utf-8

      - name: 6. 检查输出文件
        run: |
          if [ ! -f "output/epg.xml" ] || [ ! -f "output/epg.gz" ]; then
            echo "❌ 目标文件生成失败"
            exit 1
          else
            echo "✅ 目标文件生成成功"
          fi

      - name: 7. 提交并推送更新到GitHub
        run: |
          set -x
          git config --global user.name "GitHub Actions"
          git config --global user.email "actions@github.com"
          git rm -r --cached output/epg.xml output/epg.gz || true
          git add -f output/epg.xml output/epg.gz epg_merge.log
          if git diff --cached --quiet; then
            echo "ℹ️ 无EPG文件变更，跳过GitHub提交"
          else
            git commit -m "自动更新EPG数据（$(date +'%Y-%m-%d %H:%M:%S')）"
            git push origin master --force
            echo "✅ GitHub推送成功"
          fi
          set +x

      # ======================= 关键修改 2: Gitee 同步容错 =======================
      - name: 8. 🟠 同步更新到Gitee仓库 (失败仅提示)
        env:
          GITEE_SSH_PRIVATE_KEY: ${{ secrets.GITEE_SSH_PRIVATE_KEY }}
          GITEE_REPO: "git@gitee.com:room-efficiency/epg.git"
        run: |
          set -x
          # 配置SSH
          mkdir -p ~/.ssh
          echo "$GITEE_SSH_PRIVATE_KEY" > ~/.ssh/id_ed25519
          chmod 600 ~/.ssh/id_ed25519
          ssh-keyscan gitee.com >> ~/.ssh/known_hosts 2>/dev/null || true
          
          # 添加Gitee远程仓库并推送
          git remote add gitee $GITEE_REPO || true
          if git push gitee master --force; then
            echo "✅ 成功同步到Gitee仓库"
          else
            echo "⚠️ Gitee同步失败 (网络波动或密钥问题)，流程继续"
          fi
          set +x
        # 核心配置：即使Gitee推送失败，也标记这一步为"成功"，不中断主流程
        continue-on-error: true

      # ======================= 关键修改 3: 清理逻辑 =======================
      - name: 9. 🟢 清理工作目录
        # 不需要 if: always()，因为上面两步都加了 continue-on-error
        # 如果你想确保这一步总是运行，可以加上 if: ${{ always() }}
        run: |
          rm -rf __pycache__
          rm -f epg_merge.log
          rm -rf output/__pycache__
